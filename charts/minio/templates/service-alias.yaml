{{/*
  Alias service so that in-cluster traffic to s3.localhost:30080
  (via CoreDNS rewrite) reaches MinIO S3 API.
*/}}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-alias
spec:
  type: ClusterIP
  ports:
    - port: 30080
      targetPort: 9000
  selector:
    app: minio
