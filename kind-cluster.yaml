kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
    kubeadmConfigPatches:
      - |
        kind: InitConfiguration
        nodeRegistration:
          kubeletExtraArgs:
            node-labels: "ingress-ready=true"
            # Helps with Starrocks
            pod-pids-limit: "16384"
    extraPortMappings:
      # Envoy Gateway HTTP
      - containerPort: 30080
        hostPort: 30080
        listenAddress: "127.0.0.1"
        protocol: TCP
      # Envoy Gateway HTTPS
      - containerPort: 30443
        hostPort: 30443
        listenAddress: "127.0.0.1"
        protocol: TCP
      # StarRocks MySQL protocol (not HTTP, so bypasses Envoy Gateway)
      - containerPort: 30930
        hostPort: 30930
        listenAddress: "127.0.0.1"
        protocol: TCP
